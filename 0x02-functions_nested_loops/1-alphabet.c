#include "main.h"

/**
 * print_alphabet  - a function to print alphabets
 *
 * Return: always 0
 */

void print_alphabet(void)
{
	char f;

	for (f = 'a'; f <= 'z'; f++)
	_putchar(f);
	_putchar('\n');
}
