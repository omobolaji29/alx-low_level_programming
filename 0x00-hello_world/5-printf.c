#include <stdio.h>

/**
 * main - this is the function that prints a line using printf
 *
 * return: 0 when successful
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
